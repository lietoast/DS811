#include <stdlib.h>
#include <stdio.h>

#include "bi_tree.h"

int main(void)
{
	bi_tree_t a, b, x, y, z;

	create_bitree(&a);
	create_bitree(&b);
	create_bitree(&x);
	create_bitree(&y);
	create_bitree(&z);


	
	exit(0);
}
